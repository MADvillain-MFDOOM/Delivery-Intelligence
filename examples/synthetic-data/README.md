# Synthetic data examples

This directory is reserved for small, fabricated datasets and fixtures that demonstrate the public model without connecting to Jira.

Examples must use invented project names, issue keys, people, dates, estimates, and outcomes. Do not anonymise a production export and commit it; create synthetic records instead. Keep example files minimal, explain their schema, and document any assumptions needed to reproduce a result.
